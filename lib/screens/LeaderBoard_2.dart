import 'package:flutter/material.dart';

class LeaderBoard2 extends StatelessWidget {
  const LeaderBoard2({super.key});

  @override
  Widget build(BuildContext context) {
    return GestureDetector(child: const Scaffold());
  }
}
